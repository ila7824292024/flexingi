* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Roboto Mono', monospace;
    background: #0a0a0a;
    color: #00ff00;
    line-height: 1.6;
    overflow-x: hidden;
    background-image: 
        radial-gradient(circle at 1px 1px, rgba(0, 255, 0, 0.1) 1px, transparent 0),
        radial-gradient(circle at 1px 1px, rgba(0, 255, 0, 0.05) 1px, transparent 0);
    background-size: 20px 20px, 40px 40px;
    background-position: 0 0, 10px 10px;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

/* Эффекты */
.scanlines {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to bottom, 
        rgba(0, 255, 0, 0.03) 50%, 
        transparent 50%);
    background-size: 100% 4px;
    pointer-events: none;
    z-index: 999;
}

.noise {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSI0MCIgaGVpZ2h0PSI0MCI+PHJlY3Qgd2lkdGg9IjQwIiBoZWlnaHQ9IjQwIiBmaWxsPSIjMDAwMDAwIi8+PHJlY3QgeD0iMCIgeT0iMCIgd2lkdGg9IjEiIGhlaWdodD0iMSIgZmlsbD0icmdiYSgwLCAyNTUsIDAsIDAuMDMpIi8+PC9zdmc+');
    pointer-events: none;
    z-index: 998;
}

/* Заголовок */
.terminal-header {
    text-align: center;
    padding: 3rem 0;
    border-bottom: 1px solid #00ff00;
    position: relative;
}

.glitch {
    font-family: 'Press Start 2P', cursive;
    font-size: 3rem;
    color: #00ff00;
    position: relative;
    display: inline-block;
}

.glitch::before,
.glitch::after {
    content: attr(data-text);
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}

.glitch::before {
    animation: glitch-1 2s infinite linear alternate-reverse;
    color: #ff00ff;
    z-index: -1;
}

.glitch::after {
    animation: glitch-2 3s infinite linear alternate-reverse;
    color: #00ffff;
    z-index: -2;
}

.subtitle {
    color: #00cc00;
    margin-top: 1rem;
    font-size: 1.2rem;
}

/* Навигация */
.terminal-nav {
    padding: 2rem 0;
    border-bottom: 1px solid #00ff00;
}

.terminal-nav ul {
    display: flex;
    justify-content: center;
    list-style: none;
    gap: 2rem;
    flex-wrap: wrap;
}

.terminal-nav a {
    color: #00ff00;
    text-decoration: none;
    padding: 0.5rem 1rem;
    border: 1px solid #00ff00;
    transition: all 0.3s;
}

.terminal-nav a:hover {
    background: #00ff00;
    color: #000;
}

/* Основной контент */
.terminal-main {
    padding: 3rem 0;
}

.terminal-section {
    margin-bottom: 4rem;
    padding: 2rem;
    border: 1px solid #00ff00;
    background: rgba(0, 0, 0, 0.7);
    position: relative;
}

.terminal-section::before {
    content: '>';
    position: absolute;
    top: -15px;
    left: 20px;
    background: #0a0a0a;
    padding: 0 10px;
    color: #00ff00;
}

.terminal-section h2 {
    color: #00ff00;
    margin-bottom: 2rem;
    font-size: 1.5rem;
}

/* Анимация печатания */
.typewriter {
    overflow: hidden;
    border-right: 2px solid #00ff00;
    white-space: nowrap;
    animation: typing 3s steps(40, end), blink-caret 0.75s step-end infinite;
}

.typewriter p {
    animation: fadeIn 0.5s ease-in;
    margin-bottom: 0.5rem;
}

/* Списки аномалий и фракций */
.anomaly-list,
.faction-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
}

.anomaly-item,
.faction-card {
    padding: 1.5rem;
    border: 1px solid #00ff00;
    background: rgba(0, 255, 0, 0.05);
    position: relative;
}

.anomaly-item::before,
.faction-card::before {
    content: '>';
    position: absolute;
    top: -10px;
    left: 10px;
    background: #0a0a0a;
    padding: 0 5px;
    color: #00ff00;
}

/* Карта */
.zone-map {
    position: relative;
    margin-top: 2rem;
    text-align: center;
}

.ascii-art {
    color: #00ff00;
    font-size: 8px;
    line-height: 1;
    margin: 0 auto;
    display: inline-block;
}

.map-markers {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}

.marker {
    position: absolute;
    color: #ff0000;
    font-size: 0.8rem;
    transform: translate(-50%, -50%);
}

/* Футер */
.terminal-footer {
    padding: 2rem 0;
    border-top: 1px solid #00ff00;
    text-align: center;
    background: rgba(0, 0, 0, 0.8);
}

/* Анимации */
@keyframes glitch-1 {
    0% { transform: translate(0); }
    20% { transform: translate(-2px, 2px); }
    40% { transform: translate(-2px, -2px); }
    60% { transform: translate(2px, 2px); }
    80% { transform: translate(2px, -2px); }
    100% { transform: translate(0); }
}

@keyframes glitch-2 {
    0% { transform: translate(0); }
    20% { transform: translate(2px, -2px); }
    40% { transform: translate(2px, 2px); }
    60% { transform: translate(-2px, -2px); }
    80% { transform: translate(-2px, 2px); }
    100% { transform: translate(0); }
}

@keyframes typing {
    from { width: 0; }
    to { width: 100%; }
}

@keyframes blink-caret {
    from, to { border-color: transparent; }
    50% { border-color: #00ff00; }
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

/* Адаптивность */
@media (max-width: 768px) {
    .glitch {
        font-size: 2rem;
    }
    
    .terminal-nav ul {
        flex-direction: column;
        align-items: center;
        gap: 1rem;
    }
    
    .anomaly-list,
    .faction-grid {
        grid-template-columns: 1fr;
    }
    
    .ascii-art {
        font-size: 6px;
    }
}

/* Эффект мерцания */
.blink {
    animation: blink 2s infinite;
}

@keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
}
